# Liffy

AI-powered peer code review with FastAPI + Celery backend and React frontend.

## Quick start

```bash
cd /home/runner/work/Liffy/Liffy/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
PYTHONPATH=/home/runner/work/Liffy/Liffy/backend pytest
```
